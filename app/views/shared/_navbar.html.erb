<nav class="w-full flex bg-black items-center p-5 justify-between fixed">
    <div class="flex gap-5 items-center">
        <%= image_tag('librojpeg', class: "w-12")%>
        <h3 class="font-bold text-lg text-white">The bookstore App.</h3>
    </div>

    <div class="flex">
        <%= turbo_frame_tag 'new_record_component', class: "flex gap-5" do %>
            <%= link_to new_sell_path, class: "text-white font-semibold text-sm text-center bg-green-700 rounded p-2" do %>
                add sell
            <% end %>

            <%= link_to new_book_path, class: "text-white font-semibold text-sm text-center bg-sky-700 rounded p-2" do %>
                Add new book
            <% end %>
        <% end %>
    </div>

</nav>